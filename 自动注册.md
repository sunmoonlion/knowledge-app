# Casdoor 自助注册（用户端开放 / 管理端不开放）

本文约定：**仅用户端（web）开放 Casdoor 自助注册**；**管理端（admin）不提供自助注册**，后台账号由 Casdoor 管理员创建或通过既有流程下发。技术形态与现有 **OIDC 授权码 + BFF 会话** 一致，注册本身在 Casdoor 完成，业务后端不因「注册」单独再造一套协议。

---

## 1. 策略总览

| 维度 | 用户端 `app-knowledge-web` | 管理端 `app-knowledge-admin` |
|------|----------------------|-------------------------|
| Casdoor 自助注册 | **开启**（按本节配置） | **关闭** |
| BFF「去注册」跳转 | **建议提供** `GET /api/auth/signup`（见 §4） | **不提供**；登录页**不要**放「注册」链到 Casdoor |
| 账号来源 | 用户自助注册 + 可选管理员补录 | 仅管理员在 Casdoor 中创建/邀请 |
| 首登业务侧 | 现有 **影子用户 upsert**（登录回调后）不变 | 同上 |

**原则**：注册与登录在 Casdoor 侧区分；OAuth 流程始终是 `authorize → code → token → BFF session`。新用户注册成功后，仍通过 **`/api/auth/login`** 走授权码登录（与老用户相同）。

---

## 2. Casdoor 配置（操作清单）

### 2.1 用户端 Application（如 `app-knowledge-web`）

1. 打开 **Application** → 选中与 `CASDOOR_APPLICATION` 一致的应用（与 `knowledge-web-backend` 的 `.env` 对齐）。
2. 开启 **自助注册 / Sign up**（具体开关名称随 Casdoor 版本可能为 *Enable sign up*、*Self-service registration* 等，以界面为准）。
3. 配置 **Sign-up items**（注册页字段）：用户名、邮箱、密码、用户协议、邮箱验证等，与业务需要的 `openid profile email` 一致。
4. 配置 **Sign-in methods**：与注册后登录方式一致（如密码 + 邮箱验证码）。
5. **默认角色 / 权限**：新用户只能获得 **普通用户** 所需权限；与 Nest 中 `id_token` → `casdoorPermissions` 及 `@RequirePermission` 对齐，**禁止**默认赋予管理类 permission。
6. **Redirect URL**：与 `CASDOOR_REDIRECT_URI` **完全一致**（含协议、主机、端口、路径），例如：  
   `http(s)://<web-bff-host>/api/auth/callback`。
7. 配置 **邮件 / 短信 / 验证码**（若注册项启用了邮箱或手机验证）。

参考文档：[Application 配置](https://casdoor.org/docs/application/config)、[Sign-up items](https://casdoor.github.io/docs/application/signup-items-table)。

### 2.2 管理端 Application（如 `app-knowledge-admin`）

1. **关闭**该 Application 的自助注册（或与 Organization 策略一致，确保匿名用户无法在 Casdoor 上为 admin 应用创建账号）。
2. 保持 **`CASDOOR_REDIRECT_URI`** 与 admin BFF 回调一致（如 `http(s)://<admin-bff-host>/api/auth/callback`，以实际路由为准）。
3. 后台账号仅在 Casdoor **用户管理**中创建，并授予 **管理端**角色/权限。

---

## 3. 注册页 URL（与 `.env` 对齐）

Casdoor 常见自助注册入口形态为（**以你方 Casdoor 实例登录页上的「注册」链接为准**，下列为常见拼法）：

```text
{CASDOOR_ENDPOINT}/signup/{CASDOOR_ORGANIZATION}/{CASDOOR_APPLICATION}
```

示例（仅说明格式，勿照抄生产值）：

```text
https://casdoor.example.com/signup/built-in/app-knowledge-web
```

其中 `CASDOOR_ORGANIZATION`、`CASDOOR_APPLICATION` 与 `knowledge-web-backend/app/.env`（或 `.env.example`）中变量一致。

若升级 Casdoor 后路径变化，以 **Casdoor 管理后台 / 浏览器地址栏** 实际注册页 URL 为准，再写入 BFF 的 `buildSignupUrl()`（§4）。

---

## 4. 用户端 BFF：建议新增 `GET /api/auth/signup`

**目的**：前端只依赖自己的 BFF 域名，不把 Casdoor 拼 URL 逻辑散落在 Next.js；多环境时只改后端环境变量。

**行为**：`302` 重定向到 §3 的注册页 URL（或 Casdoor 当前版本文档推荐的等价 URL）。

**实现位置**（与现有代码一致）：

- `knowledge-web-backend/app/src/access-control/auth/auth.service.ts`：新增 `buildSignupUrl(): string`（读取 `CASDOOR_ENDPOINT`、`CASDOOR_ORGANIZATION`、`CASDOOR_APPLICATION`，拼路径后 `encodeURIComponent` 按需处理）。
- `knowledge-web-backend/app/src/access-control/auth/auth.controller.ts`：新增 `@Public() @Get('signup')`，`res.redirect(this.authService.buildSignupUrl())`。

**注意**：全局前缀默认为 `/api`，故对外路径为 **`GET /api/auth/signup`**（与 `CASDOOR_REDIRECT_URI` 中的 `/api/auth/callback` 一致）。

**管理端**：**不要**在 `knowledge-admin-backend` 增加对等的 `signup` 路由；避免误导向管理端应用的注册页。

---

## 5. 前端（用户端）

- 在登录页（如 `knowledge-web-frontend` 的 `zh-CN/login`）增加 **「注册」** 链接：
  - 推荐：`href` 指向 **`{WEB_BFF_ORIGIN}/api/auth/signup`**（与当前 `NEXT_PUBLIC_*` / 请求基址配置一致），由 BFF 再跳 Casdoor。
- **管理端前端**：登录页**仅保留「登录」**；不展示自助注册入口。

若前端存在 **Next.js 内置 BFF**（`NEXT_PUBLIC_BFF_PROVIDER=frontend`）模式，需在对应 `app/api/auth/` 下增加等价的 `signup` 路由，与本文策略一致；当前仓库以 **后端 BFF** 为主时，以 Nest 为准。

---

## 6. 注册完成后的用户路径

1. 用户在 Casdoor 完成注册（及邮箱验证等）。
2. 用户打开站点，点击 **登录** → `GET /api/auth/login` → Casdoor `authorize` → 回调 `callback` → 换 token → 写 Redis session → 与现网一致。
3. **首登**：`AuthService.exchangeCodeForSession` 内已有 **影子用户同步**，无需为注册单独加接口。

---

## 7. 安全与运维检查清单

- [ ] `app-knowledge-web` 默认角色 **无** 管理端 permission。
- [ ] `app-knowledge-admin` **已关闭**自助注册。
- [ ] 生产环境 Redirect URL 与 `CASDOOR_REDIRECT_URI` **逐字符一致**。
- [ ] 日志中勿打印 `client_secret`、完整 `id_token`。
- [ ] 需要风控时：在 Casdoor 侧限制注册频率、强制邮箱验证、或接入审批流。

---

## 8. 与仓库其它文档的关系

- 总体 BFF 与 Casdoor 关系：见根目录 **`README.md`**「当前认证与权限架构」。
- Web BFF 路由与 Cookie：见 **`knowledge-web-backend/CLAUDE.md`**、`auth.controller.ts` 注释。
- Admin 无自助注册：本文 §1、§2.2、§5 为**权威约定**；若与旧草稿冲突，以本文为准。

---

## 9. 修订记录

| 日期 | 说明 |
|------|------|
| 2026-04-10 | 初版：web 开放注册 + admin 不开放；Casdoor 配置清单；建议 `/api/auth/signup` 与前端链接。 |
