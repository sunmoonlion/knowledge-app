---
description: 管理端 Next.js（knowledge-admin-frontend）
globs: **/knowledge-admin-frontend/**/*.{tsx,ts,jsx,js}
alwaysApply: false
---

# Admin Frontend

- 技术栈以当前 Next.js/React/Ant Design 依赖为准。
- 内部治理操作只消费统一 Backend 的 Admin API。
- 复用模板通用能力；请求、鉴权与错误处理集中封装。
- 页面覆盖 loading/error/empty/success，避免空值导致白屏。
- 不把 access token 写入 `localStorage`，遵循既定 BFF/session 模式。
