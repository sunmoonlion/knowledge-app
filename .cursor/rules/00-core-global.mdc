---
description: knowledge-app 架构 v2 三子模块父仓全局协作规则
alwaysApply: true
---

# knowledge-app 全局规则

- 活跃源码只允许 `knowledge-admin-frontend`、`knowledge-web-frontend`、`knowledge-backend`。
- Admin/Web 前端共用统一 FastAPI Backend，不得重新引入独立 Web Backend。
- Worker 代码属于统一 Backend，K8s 声明属于 k8s 仓库，父仓不得保存副本。
- 接口、鉴权、错误码和 schema 以后端代码/OpenAPI/contract tests 为准。
- 禁止提交明文凭据；每个改动必须有可重复验证和可追溯提交。
- 推送先子仓、后父仓；历史冻结材料不得冒充当前实现。
