---
description: 统一 FastAPI Backend（knowledge-backend）
globs: **/knowledge-backend/**/*.py
alwaysApply: false
---

# 统一 Backend

- `application/admin`、`application/web`、`application/internal` 保持入口与授权边界。
- 领域与基础设施共享，不按前端复制服务、数据库模型或认证实现。
- 配置通过环境变量或 `pydantic-settings` 注入，禁止写死凭据。
- 新接口必须同步 schema/contract tests，并覆盖成功与常见失败路径。
- Worker 是同一代码基线的独立运行角色，不在父仓复制源码。
