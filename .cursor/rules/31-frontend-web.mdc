---
description: 用户端 Next.js（knowledge-web-frontend）
globs: **/knowledge-web-frontend/**/*.{tsx,ts,jsx,js}
alwaysApply: false
---

# Web Frontend

- 技术栈以当前 Next.js/React/shadcn-ui 依赖为准。
- 只消费统一 Backend 的 Web API；内部治理能力不得暴露到用户端。
- SSR/BFF 路由可做会话与前端适配，不建立第二套领域 Backend。
- 请求、鉴权与错误处理集中封装，页面覆盖完整状态。
- standalone 构建路径变更必须同步 Dockerfile 并运行镜像冒烟。
