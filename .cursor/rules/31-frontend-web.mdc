---
description: 用户端 Next.js（knowledge-web-frontend，源码多在 app/）
globs: **/*web-frontend/**/*.{tsx,ts,jsx}
alwaysApply: false
---

# 前端（用户端 SSR / Next.js）

- 技术栈：Next.js + React + shadcn/ui 等以 `app/package.json` 为准；内置 BFF 路由见项目内 `/api/auth/` 等实现。
- 先联调后美化：先打通接口与导航，再抠样式细节。
- 鉴权：遵循模板既定模式（Casdoor BFF / session）；不随意把 access token 写入 `localStorage`，除非文档明确约定。
- 请求封装：优先使用项目已有 axios/fetch 封装与 React Query 等模式，避免页面内重复造轮子。
- 与后端对齐：字段与错误码以后端及 `API_CONTRACT` 为准。
- 字段容错：对后端空值做兜底，避免渲染崩溃。
- 状态可追踪：关键页面覆盖 loading / error / empty / success。
- Docker/standalone：生产镜像依赖 `next.config` 的 `output: 'standalone'` 时，变更构建产物路径需同步 `mybuild/Dockerfile`。
