apiVersion: v1
kind: Namespace
metadata:
  name: ${NAMESPACE}
  labels:
    app: app-platform
    component: nodebullworker-knowledge-web-backend
    environment: ${ENVIRONMENT}
    managed-by: deploy-script
